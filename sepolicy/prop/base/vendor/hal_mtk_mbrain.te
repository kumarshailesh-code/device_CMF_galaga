# ==============================================
# MTK Policy Rule
# ==============================================

hal_attribute_service(hal_mtk_mbrain, mtk_hal_mbrain_service)

binder_call(hal_mtk_mbrain_client, hal_mtk_mbrain_server)
binder_call(hal_mtk_mbrain_server, hal_mtk_mbrain_client)
binder_call(hal_mtk_mbrain_server, servicemanager)
